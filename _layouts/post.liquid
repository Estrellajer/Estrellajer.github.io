---
layout: default
---
{% assign year = page.date | date: '%Y' %}
{% assign url_beginning = page.url | slice: 0, 6 %}
{% assign has_outline = false %}
{% if content contains '<h1' or content contains '<h2' or content contains '<h3' or content contains '<h4' %}
  {% assign has_outline = true %}
{% endif %}

{% if page._styles %}
  <style type="text/css">
    {{ page._styles }}
  </style>
{% endif %}

<style>
  .layout-post #navbar > .container {
    max-width: 1320px;
  }

  .layout-post .page-container {
    max-width: 1320px;
  }

  .reading-page {
    --reading-text: var(--global-text-color);
    --reading-muted: #69717d;
    --reading-soft: #8d96a3;
    --reading-accent: #557a9f;
    --reading-accent-soft: rgba(85, 122, 159, 0.12);
    width: min(100%, 82rem);
    margin: 0 auto;
    padding-top: 1.15rem;
  }

  .reading-shell {
    display: grid;
    grid-template-columns: minmax(0, 1fr) 240px;
    gap: 2.4rem;
    align-items: start;
  }

  .reading-shell--single {
    grid-template-columns: minmax(0, 1fr);
    max-width: 68rem;
  }

  .reading-main {
    min-width: 0;
  }

  .reading-hero {
    margin-bottom: 2rem;
  }

  .reading-eyebrow {
    margin-bottom: 0.9rem;
    color: var(--reading-accent);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .reading-title {
    margin: 0;
    font-size: clamp(2.45rem, 4.8vw, 4.1rem);
    line-height: 1.03;
    letter-spacing: -0.045em;
    font-weight: 600;
    overflow-wrap: anywhere;
  }

  .reading-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem 1rem;
    margin-top: 1rem;
    color: var(--reading-muted);
    font-size: 0.98rem;
    line-height: 1.8;
  }

  .reading-taxonomy {
    display: flex;
    flex-wrap: wrap;
    gap: 0.7rem;
    margin-top: 1.25rem;
  }

  .reading-chip {
    display: inline-flex;
    align-items: center;
    min-height: 2.2rem;
    padding: 0 0.92rem;
    border: 1px solid rgba(85, 122, 159, 0.12);
    border-radius: 999px;
    background: var(--reading-accent-soft);
    color: #496885;
    font-size: 0.9rem;
    text-decoration: none !important;
  }

  .reading-sidebar {
    min-width: 0;
  }

  .reading-sidebar[hidden] {
    display: none;
  }

  .reading-sidebar-inner {
    position: sticky;
    top: 6rem;
    padding: 0.8rem 0 0 1.1rem;
    border-left: 1px solid rgba(17, 19, 23, 0.08);
  }

  .reading-sidebar-label {
    margin-bottom: 0.8rem;
    color: var(--reading-soft);
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  .reading-toc,
  .reading-toc ul {
    padding: 0;
    margin: 0;
    list-style: none;
  }

  .reading-toc ul {
    display: grid;
    gap: 0.3rem;
  }

  .reading-toc > ul > li + li {
    margin-top: 0.4rem;
  }

  .reading-toc a {
    display: block;
    padding-left: 0.5rem;
    border-left: 2px solid transparent;
    color: var(--reading-muted);
    font-size: 0.94rem;
    line-height: 1.6;
    text-decoration: none;
    transition:
      color 0.18s ease,
      border-color 0.18s ease;
  }

  .reading-toc ul ul {
    margin-top: 0.35rem;
    padding-left: 0.95rem;
  }

  .reading-toc a:hover {
    color: var(--reading-accent);
    border-left-color: rgba(85, 122, 159, 0.28);
  }

  .reading-toc a.is-active {
    color: #223243;
    border-left-color: rgba(85, 122, 159, 0.55);
  }

  .reading-toc li[data-level='1'] > a {
    color: #223243;
    font-weight: 600;
  }

  .reading-toc li[data-level='2'] > a {
    font-size: 0.91rem;
  }

  .reading-toc li[data-level='3'] > a,
  .reading-toc li[data-level='4'] > a {
    color: var(--reading-soft);
    font-size: 0.86rem;
  }

  .reading-article,
  .reading-article #markdown-content {
    color: var(--reading-text);
    font-size: 1.1rem;
    line-height: 1.95;
  }

  .reading-article #markdown-content > p,
  .reading-article #markdown-content > ul,
  .reading-article #markdown-content > ol,
  .reading-article #markdown-content > blockquote,
  .reading-article #markdown-content > pre,
  .reading-article #markdown-content > figure,
  .reading-article #markdown-content > table,
  .reading-article #markdown-content > .table-responsive {
    margin-top: 1.2rem;
    margin-bottom: 1.2rem;
  }

  .reading-article #markdown-content > :first-child {
    margin-top: 0;
  }

  .reading-article #markdown-content > * {
    max-width: 100%;
  }

  .reading-article img {
    margin: 1.4rem 0;
    border-radius: 1rem;
  }

  .reading-article h1,
  .reading-article h2,
  .reading-article h3,
  .reading-article h4 {
    margin-top: 2.5rem;
    margin-bottom: 1rem;
    color: #1a2632;
    letter-spacing: -0.028em;
    line-height: 1.34;
    scroll-margin-top: 6.5rem;
  }

  .reading-article [data-reading-level='1'] {
    margin-top: 3rem;
    padding-top: 0.2rem;
    padding-bottom: 0.68rem;
    border-bottom: 1px solid rgba(17, 19, 23, 0.08);
    color: #162330;
    font-size: clamp(1.96rem, 2.15vw, 2.3rem);
    font-weight: 700;
    line-height: 1.28;
  }

  .reading-article [data-reading-level='2'] {
    margin-top: 2.7rem;
    color: #203244;
    font-size: clamp(1.44rem, 1.7vw, 1.68rem);
    font-weight: 700;
    line-height: 1.4;
  }

  .reading-article [data-reading-level='3'] {
    margin-top: 2.1rem;
    color: #304558;
    font-size: 1.28rem;
    font-weight: 680;
    letter-spacing: -0.01em;
  }

  .reading-article [data-reading-level='4'] {
    margin-top: 1.8rem;
    color: #4f6478;
    font-size: 1.08rem;
    font-weight: 650;
    letter-spacing: 0;
  }

  .reading-article p {
    color: #3f4854;
  }

  .reading-article strong {
    color: #202833;
    font-weight: 650;
  }

  .reading-article blockquote {
    margin: 1.6rem 0;
    padding: 0.45rem 0 0.45rem 1.2rem;
    border-left: 2px solid rgba(85, 122, 159, 0.34);
    color: var(--reading-muted);
    background: linear-gradient(90deg, rgba(85, 122, 159, 0.08), rgba(85, 122, 159, 0));
    border-radius: 0 0.9rem 0.9rem 0;
  }

  .reading-article ul,
  .reading-article ol {
    padding-left: 1.55rem;
  }

  .reading-article li + li {
    margin-top: 0.45rem;
  }

  .reading-article li::marker {
    color: #6d8eaf;
  }

  .reading-article pre,
  .reading-article figure.highlight,
  .reading-article div.highlighter-rouge,
  .reading-article .highlighter-rouge .highlight {
    overflow-x: auto;
    background: #f6f8fb !important;
    border: 1px solid rgba(17, 19, 23, 0.08);
    box-shadow: none;
  }

  .reading-article pre {
    margin: 1.4rem 0;
    border-radius: 1rem;
    padding: 1.05rem 1.2rem;
  }

  .reading-article figure.highlight {
    margin: 1.4rem 0;
    padding: 1.05rem 1.2rem;
    border-radius: 1rem;
  }

  .reading-article div.highlighter-rouge {
    margin: 1.4rem 0;
    padding: 1.05rem 1.2rem;
    border-radius: 1rem;
  }

  .reading-article .highlighter-rouge .highlight {
    margin: 0;
    padding: 0;
    border: 0;
    border-radius: 0;
    background: transparent !important;
  }

  .reading-article figure.highlight pre,
  .reading-article div.highlighter-rouge pre,
  .reading-article .highlighter-rouge .highlight pre {
    margin: 0;
    padding: 0;
    border: 0;
    border-radius: 0;
    background: transparent !important;
    box-shadow: none;
  }

  .reading-article figure.highlight code,
  .reading-article div.highlighter-rouge code,
  .reading-article .highlighter-rouge .highlight code {
    display: block;
    border: 0;
    border-radius: 0;
    background: transparent !important;
    color: inherit;
  }

  .reading-article pre code {
    padding: 0;
    background: transparent !important;
    border: 0;
    color: inherit;
  }

  .reading-article code {
    padding: 0.16rem 0.4rem;
    border: 1px solid rgba(85, 122, 159, 0.14);
    border-radius: 0.45rem;
    background: rgba(85, 122, 159, 0.08) !important;
    color: #35506b;
    font-size: 0.92em;
  }

  .reading-article hr {
    margin: 2.2rem 0;
    border: 0;
    border-top: 1px solid rgba(17, 19, 23, 0.08);
  }

  .reading-article table {
    width: 100%;
    overflow: hidden;
    border-collapse: separate;
    border-spacing: 0;
    border: 1px solid rgba(17, 19, 23, 0.08);
    border-radius: 0.95rem;
    background: #fbfcfd;
  }

  .reading-article th,
  .reading-article td {
    padding: 0.85rem 1rem;
    border-bottom: 1px solid rgba(17, 19, 23, 0.06);
    text-align: left;
    vertical-align: top;
  }

  .reading-article th {
    color: #223243;
    font-weight: 650;
    background: rgba(85, 122, 159, 0.08);
  }

  .reading-article tr:last-child td {
    border-bottom: 0;
  }

  .reading-article a {
    color: inherit;
    text-decoration-color: rgba(85, 122, 159, 0.45);
  }

  .reading-article a:hover {
    text-decoration-color: rgba(85, 122, 159, 0.9);
  }

  .reading-related {
    margin-top: 3rem;
    padding-top: 1.6rem;
    border-top: 1px solid rgba(17, 19, 23, 0.08);
  }

  .reading-related-title {
    margin: 0 0 1rem;
    font-size: 1.2rem;
    font-weight: 600;
    letter-spacing: -0.02em;
  }

  .reading-related-list {
    display: grid;
    gap: 0.9rem;
    padding: 0;
    margin: 0;
    list-style: none;
  }

  .reading-related-item a {
    display: block;
    color: inherit;
    text-decoration: none;
  }

  .reading-related-item-title {
    font-weight: 600;
    line-height: 1.45;
  }

  .reading-related-item-meta {
    margin-top: 0.2rem;
    color: var(--reading-soft);
    font-size: 0.92rem;
    line-height: 1.7;
  }

  @media (max-width: 980px) {
    .reading-shell {
      grid-template-columns: 1fr;
      gap: 1.8rem;
    }

    .reading-sidebar-inner {
      position: static;
      padding: 1rem 0 0;
      border-top: 1px solid rgba(17, 19, 23, 0.08);
      border-left: 0;
    }
  }
</style>

<div class="reading-page">
  <div class="reading-shell{% unless has_outline %} reading-shell--single{% endunless %}">
    <div class="reading-main">
      <header class="reading-hero">
        <div class="reading-eyebrow">{% if url_beginning == '/blog/' %}Blog Post{% else %}Article{% endif %}</div>
        <h1 class="reading-title">{{ page.title }}</h1>
        <div class="reading-meta">
          <span>{{ page.date | date: '%B %d, %Y' }}</span>
          {% if page.author %}<span>{{ page.author }}</span>{% endif %}
          {% if page.last_updated %}<span>Updated {{ page.last_updated | date: '%B %d, %Y' }}</span>{% endif %}
          {% if page.meta %}<span>{{ page.meta }}</span>{% endif %}
        </div>
        <div class="reading-taxonomy">
          {% if url_beginning == '/blog/' %}
            <a class="reading-chip" href="{{ year | prepend: '/blog/' | relative_url }}">{{ year }}</a>
          {% else %}
            <span class="reading-chip">{{ year }}</span>
          {% endif %}
          {% for tag in page.tags %}
            {% if url_beginning == '/blog/' %}
              <a class="reading-chip" href="{{ tag | slugify | prepend: '/blog/tag/' | relative_url }}"># {{ tag }}</a>
            {% else %}
              <span class="reading-chip"># {{ tag }}</span>
            {% endif %}
          {% endfor %}
          {% for category in page.categories %}
            {% if url_beginning == '/blog/' %}
              <a class="reading-chip" href="{{ category | slugify | prepend: '/blog/category/' | relative_url }}">{{ category }}</a>
            {% else %}
              <span class="reading-chip">{{ category }}</span>
            {% endif %}
          {% endfor %}
        </div>
      </header>

      <article class="reading-article">
        <div id="markdown-content">
          {{ content }}
        </div>
      </article>

      {% if page.citation %}
        {% include citation.liquid %}
      {% endif %}

      {% if page.related_publications %}
        <h2>References</h2>
        <div class="publications">
          {% bibliography --group_by none --cited_in_order %}
        </div>
      {% endif %}

      {% assign related_items = page.related_posts %}
      {% if site.related_blog_posts and site.related_blog_posts.enabled and related_items and related_items != false and related_items.size > 0 %}
        <section class="reading-related">
          <h2 class="reading-related-title">Further Reading</h2>
          <ul class="reading-related-list">
            {% for related in related_items limit: 4 %}
              <li class="reading-related-item">
                <a href="{{ related.url | relative_url }}">
                  <div class="reading-related-item-title">{{ related.title }}</div>
                  <div class="reading-related-item-meta">
                    {{ related.date | date: '%b %d, %Y' }}
                    {% if related.description %} / {{ related.description }}{% endif %}
                  </div>
                </a>
              </li>
            {% endfor %}
          </ul>
        </section>
      {% endif %}
    </div>

    {% if has_outline %}
      <aside class="reading-sidebar" data-reading-sidebar>
        <div class="reading-sidebar-inner">
          <div class="reading-sidebar-label">On This Page</div>
          <nav class="reading-toc" data-reading-toc></nav>
        </div>
      </aside>
    {% endif %}
  </div>

  {% if site.disqus_shortname and page.disqus_comments %}
    {% include disqus.liquid %}
  {% endif %}
  {% if site.giscus and page.giscus_comments %}
    {% include giscus.liquid %}
  {% endif %}
</div>

{% if has_outline %}
  <script>
    document.addEventListener('DOMContentLoaded', function () {
      const article = document.getElementById('markdown-content');
      const sidebar = document.querySelector('[data-reading-sidebar]');
      const toc = document.querySelector('[data-reading-toc]');

      if (!article || !sidebar || !toc) {
        return;
      }

      const headings = Array.from(article.querySelectorAll('h1, h2, h3, h4')).filter((heading) => {
        return heading.textContent && heading.textContent.trim().length > 0;
      });
      if (headings.length < 2) {
        sidebar.hidden = true;
        return;
      }

      const rawLevels = [...new Set(headings.map((heading) => Number(heading.tagName.slice(1))))].sort((a, b) => a - b);
      const levelMap = new Map(rawLevels.map((level, index) => [level, index + 1]));
      const usedIds = new Map();

      const ensureUniqueId = (value, index) => {
        const base =
          String(value || '')
            .trim()
            .toLowerCase()
            .replace(/[^\p{Letter}\p{Number}\s-]/gu, '')
            .replace(/\s+/g, '-')
            .replace(/-+/g, '-')
            .replace(/^-|-$/g, '') || `section-${index + 1}`;

        const count = usedIds.get(base) || 0;
        usedIds.set(base, count + 1);

        if (count === 0) {
          return base;
        }

        return `${base}-${count + 1}`;
      };

      headings.forEach((heading, index) => {
        heading.id = ensureUniqueId(heading.id || heading.textContent, index);

        const normalizedLevel = levelMap.get(Number(heading.tagName.slice(1))) || 1;
        heading.dataset.readingLevel = String(normalizedLevel);
      });

      const rootList = document.createElement('ul');
      const listStack = [rootList];
      let currentLevel = 1;

      headings.forEach((heading) => {
        const normalizedLevel = Number(heading.dataset.readingLevel || 1);

        while (currentLevel < normalizedLevel) {
          const parentList = listStack[listStack.length - 1];
          const lastItem = parentList.lastElementChild;

          if (!lastItem) {
            break;
          }

          const nestedList = document.createElement('ul');
          lastItem.appendChild(nestedList);
          listStack.push(nestedList);
          currentLevel += 1;
        }

        while (currentLevel > normalizedLevel) {
          listStack.pop();
          currentLevel -= 1;
        }

        const item = document.createElement('li');
        item.dataset.level = String(normalizedLevel);

        const link = document.createElement('a');
        link.href = `#${heading.id}`;
        link.textContent = heading.textContent;
        item.appendChild(link);
        listStack[listStack.length - 1].appendChild(item);
      });

      if (!rootList.children.length) {
        sidebar.hidden = true;
        return;
      }

      toc.appendChild(rootList);

      const links = Array.from(toc.querySelectorAll('a'));
      const setActiveHeading = () => {
        const offset = 140;
        let currentHeading = headings[0];

        headings.forEach((heading) => {
          if (heading.getBoundingClientRect().top <= offset) {
            currentHeading = heading;
          }
        });

        links.forEach((link) => {
          link.classList.toggle('is-active', link.getAttribute('href') === `#${currentHeading.id}`);
        });
      };

      setActiveHeading();
      document.addEventListener('scroll', setActiveHeading, { passive: true });
      window.addEventListener('resize', setActiveHeading);
    });
  </script>
{% endif %}
